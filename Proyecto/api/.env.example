PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=programacion4

DEFAULT_USER_ID=1

JWT_SECRET=cambiar-en-produccion-usar-cadena-aleatoria-larga
JWT_ACCESS_EXPIRES_IN=15m
JWT_EXPIRES_IN=8h
JWT_REFRESH_SECRET=otro-secreto-distinto-al-access
JWT_REFRESH_EXPIRES_IN=7d

# Origen del front estático (Live Server, etc.) para CORS
FRONT_ORIGIN=http://127.0.0.1:5500
